import { StyleSheet } from 'react-native';
import { Colors } from 'src/theme';

export default StyleSheet.create({
  boldFont: {
    fontWeight: 'bold',
  },
  primary: { color: Colors.primary },
  secondary: { color: Colors.white },
  dumy: { color: 'red' },
  nileblue: { color: Colors.nileblue }
});
